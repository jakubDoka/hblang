deep dedup 2 (generics).size(t = int):
	mov rax, 0x08
	ret
main:
	push rbx
	sub rsp, 0x08
	call :deep dedup 2 (generics).size(t = int)
	mov rbx, rax
	call :deep dedup 2 (generics).size(t = int)
	sub rbx, rax
	mov rax, rbx
	add rsp, 0x08
	pop rbx
	ret