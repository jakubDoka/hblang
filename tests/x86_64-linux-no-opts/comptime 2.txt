comptime 2.main.some_fn3(uint)(.some_fn(uint)):
	sub rsp, 0x08
	call :comptime 2.main.some_fn(uint)
	add rax, 0x0A
	add rsp, 0x08
	ret
comptime 2.main.some_fn3(uint)((.some_fn2(uint, .some_fn(uint)))):
	sub rsp, 0x08
	call :comptime 2.main.some_fn3(uint)(.some_fn2(uint, .some_fn(uint)))
	add rax, 0x0A
	add rsp, 0x08
	ret
comptime 2.main.some_fn3(uint)(.some_fn2(uint, .some_fn(uint))):
	sub rsp, 0x08
	call :comptime 2.main.some_fn2(uint, .some_fn(uint))
	add rax, 0x0A
	add rsp, 0x08
	ret
comptime 2.main.some_fn2(uint, .some_fn(uint)):
	sub rsp, 0x08
	call :comptime 2.main.some_fn(uint)
	inc rax
	add rsp, 0x08
	ret
comptime 2.main.some_fn(uint):
	mov rax, 0x01
	ret
main:
	push rbx
	sub rsp, 0x10
	mov dword ptr [rsp], 0x07
	mov rax, 0x90000002
	mov [rsp+0x04], eax
	mov rcx, 0x80000000
	mov rax, 0x90000003
	mov [rsp+0x08], eax
	mov [rsp+0x0C], ecx
	call :comptime 2.main.some_fn3(uint)(.some_fn(uint))
	mov rbx, rax
	call :comptime 2.main.some_fn3(uint)((.some_fn2(uint, .some_fn(uint))))
	add rbx, rax
	mov rax, rbx
	add rsp, 0x10
	pop rbx
	ret